
#### Primitive Types
这些类型是基本类型， 在Java中天然存在
int boolean char byte short double float (8)
```java
//省略版的代码

ABC a = new ABC(...); //new关键字只返回地址
b = a;
------
int x = 5;
int y;
y = x;
x = 2;
```
前面一个会把ab指向同一个对象 但是后面一段代码会把x y分别创建

 y = x 把x中的所有bits拷贝到y中 这就是为什么下面的int会有两个内存块储存数据
#### Reference Type
除了基本类型， 其他Java类型都是引用类型 包括Array， List......
对于引用类型 创建实例时产生了一个对这个实例的引用

这里的new 为这个对象开辟了一块内存块并且存储了这个对象的信息 然后新的keyword告诉这个对象存储在什么地方

 **`ABC a`**   这段代码并没有存储 ABC这个实例的信息 而是存储了一个指向这个示例的引用信息 

#### Array in Java
声明一个array
```java
int[] array = new int[]{1, 2, 3};
```
如果Array储存的不是基本类型 他们仍然会存储一个地址 指向真实的实例数据

#### List in Java

```java
import java.util.List;
import java.util.LinkedList;

List<Type of Stat> VariableName = new LinkedList<>();
```

#### SLLists
通常在Java中不把裸递归直接给用户看
普通的定义一般是一个item和一个Node
这个是SLL：
```java
//前面先行了一个IntNode的定义 一个item、一个Node
//IntNode是一个递归的数据结构 但是SLL不是

public class SLList{
	private IntNode first;//不允许用户触碰IntNode
	
	public SLList(int x){
		first = new IntNode(x, null);
	}
	/*Use IntNode to hide the recursion used in this higher class SLList*/
}
```
让用户不必考虑递归 直接构建数据结构 
事实上在加入数据和Llist中间加入了一层first
#### private 私有关键词 
只允许创建类访问private关键词后面的成员 其他类不允许访问

##### 私有辅助函数
```java
//------一些类定义
public size(){
	return size(first);
}

private int size(InteNode p){
	if (p.next == null){
		return 1;
	}
	return 1 + size(p.next);
}
```

可以做一个尺寸记忆的实例属性来在每次操作记录数据 减少运行时间
#### Nested Class

一个只在某个类中生效的另一个类的存放方式 一般设为private
比如这里的IntNode和Node这种类 本身不能被外界访问 但是在class SLList中生效

#### Optimize
1. 我们发现如果创建一个空列表 会导致p.next无法运行 尽管我们可以增加特殊情况（if）但是不符合code quality
	所以做法是： 在每个List前面增加一个哨兵值sentinal 来确保代码里的list永不为null
2. size 可以做一个尺寸记忆的实例属性来在每次操作记录数据 减少运行时间
3. add 添加一个指针指向最后一个元素 方便直接在最后一个元素处添加元素

哨兵值代码实现：
```java
/*Above are the definition and other methods*/
//首先 我们在最上面直接声明一个哨兵值 在后面每一个方法编写中都调用

private IntNode sentinel;

public addList(int x){
	this.size += 1
;	IntNode p = sentinel;
	while
	


}
```

#### 双向链表 DLList

如何针对哨兵值实现优化：1. 两个哨兵节点一头一尾 2.

实际的是 只需要一个烧饼 烧饼的是循环指向的 最后一个元素指向头部的哨兵值本身
.last & .prev

![[Pasted image 20260317172202.png]]

#### Generic泛型
```java
/*这边的Iroha就是泛型*/
public class SLList <Iroha>{
	private class Node{
		public Iroha item;
		public Node next;
	}
	
	public SLList(Iroha x){
		first = new Node(x, null);
	}
	
	public static void main(String[] args){
		SLList<String> L = new SLList<String>();
		/*这边在尖括号内输入类型 来确定定义的类的泛型的输入内容*/
	}
}
```
这边的Iroha是泛型 用来允许函数接收一个不确定类型的输入
泛型只允许接受一个Reference Type的输入 不能输入Primitive Types


