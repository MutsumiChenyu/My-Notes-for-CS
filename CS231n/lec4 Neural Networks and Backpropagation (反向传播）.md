
#### Neurals Networks
1. Linear Classifiers, f = Wx, W(C x D), x(D) -- D, features; C, classes
2. 2-layer classifier, f2 = W2(max(0, W1x)) *Add one linear layer*

##### We need non-linearity
We cannot separate multiple points with one linear cut.

---> Non-linearity transformation + linear classifiers