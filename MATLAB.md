#### 矩阵运算 
`A .* B`  逐元素相乘
`A * B` 矩阵乘法
`inv(A)`矩阵求逆
`A'`矩阵转置
`rand(row, col)生成一个特定大小的随机矩阵
`size(particularMatrix)`得到一个数组 -- [row, col] of this Matrix

#### 逻辑运算
`x = [1, 3, 7, 10];

`x > 2 & x < 8

>>> 	[F, T, T, F]


&& || 就是正常的逻辑运算
& | 为 Bitwisie运算 
