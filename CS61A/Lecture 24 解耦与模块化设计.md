
#### Modular Design
分离各个模块的实现内容实现解耦
比如：餐厅选择算法

**一个repr的用法：**
![[Pasted image 20260302170445.png||400]]
我们观察到上面有一串意义不明的输出 这个是因为我们没有给这个类添加一个repr， 当他被输出的时候解释器不知道他要怎么输出，于是默认了--所以我们要这么写：
```python
···
def __repr__(self):
	return '<' + self.name + '>'
	#告诉解释器怎么输出这个数据类型
```

##### `sorted(iter, key)`函数
接受一个可迭代对象和一个键值，返回一个**新的 根据键值从小到大排好序的 完整列表**
key可以是一个函数